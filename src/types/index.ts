export interface ISelect {
  defaultOption: string,
  optionsList: string[],
  padding?: string,
}

export interface IButtonSpan {
  width: number,
  height: number,
}

export interface ISetup {
  font: string, 
  weight: string, 
  size: string, 
  color: string, 
  lh: string, 
  ls: string, 
  textAlign: number,
}